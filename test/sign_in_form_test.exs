defmodule SignInFormTest do
  defmodule SignInForm do
    # use GoldenOrb

    # defmacro html(arg1), do: nil

    # html do
    #   form style(
    #          [col(), p(4)],
    #          md: [row()],
    #          fine: [p(2)],
    #          light: [bg(200)],
    #          dark: [bg(900)]
    #        ) do
    #     textbox("Email", :email)
    #     textbox("Password", :password)

    #     button("Sign In")
    #   end
    #   |> submit email: :trim, password: :trim do
    #     # Emits a command
    #     Auth.sign_in(email: email, password: password)
    #   end
    # end
  end
end
